server:
  port: 8888
spring:
  #profiles:
  #active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thrasht/CoeConfigRepository
          searchPaths: studentservice, professorservice, signatureservice